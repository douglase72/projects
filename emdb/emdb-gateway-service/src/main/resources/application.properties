# --------------------------------------------------------------------
# --- DEFAULT CONFIGURATION
# --------------------------------------------------------------------
quarkus.http.root-path=emdb/api
quarkus.http.port=60310
quarkus.http.cors.enabled=true
quarkus.http.cors.origins=http://localhost:5173
quarkus.http.limits.max-body-size=10M

# --------------------------------------------------------------------
# --- DEFAULT MESSAGING CONFIGURATION
# --------------------------------------------------------------------

# --------------------------------------------------------------------
# --- DEFAULT gRPC CONFIGURATION
# --------------------------------------------------------------------
quarkus.grpc.server.use-separate-server=false
quarkus.grpc.clients.media-service.host=localhost
quarkus.grpc.clients.media-service.port=9000
quarkus.grpc.clients.media-service.plain-text=true 
quarkus.grpc.clients.media-service.compression=gzip
quarkus.grpc.clients.media-service.max-inbound-message-size=10485760
