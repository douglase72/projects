# --------------------------------------------------------------------
# --- DEVELOPMENT TMDB CONFIGURATION
# --------------------------------------------------------------------
tmdb.cast.limit=1
tmdb.crew.limit=0
tmdb.rate.limit=40.0

# --------------------------------------------------------------------
# --- PRODUCTION MESSAGING CONFIGURATION
# --------------------------------------------------------------------
rabbitmq-host=rabbitmq

# --------------------------------------------------------------------
# --- PRODUCTION LOGGING CONFIGURATION
# --------------------------------------------------------------------
quarkus.log.json.log-format=ecs
quarkus.log.json.additional-field."service.name".value=emdb-ingest-service
quarkus.log.json.additional-field."service.name".type=string
